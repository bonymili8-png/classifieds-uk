{
  "name": "ogoloshennia-e2e",
  "private": true,
  "description": "Опційні E2E-тести інтерфейсу (Playwright). Не потрібні для роботи застосунку.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
