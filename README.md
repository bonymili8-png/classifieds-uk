# ОголошенняUK 🇺🇦🇬🇧

Сучасний, швидкий та адаптивний веб/мобільний застосунок (PWA) **дошки оголошень для українців у Великій Британії**.

Короткі універсальні оголошення: **ціна**, **фото** (показуються лише на сторінці самого оголошення), **телефон / WhatsApp / Telegram**, **коротка назва + опис**, **місто або пост-код** (повний чи частковий).

## Можливості

- 📱 **Адаптивний дизайн** — однаково зручно на телефоні й на десктопі (нижня таб-навігація на мобільних).
- ⚡ **Швидкий** — без важких фреймворків. Сервер на чистому Node.js, фронтенд — ванільний ES-модулі.
- 🗂️ **16 категорій** — робота, житло, послуги, транспорт, речі, меблі, електроніка, дитячі товари, краса, документи, навчання, тварини, їжа, безкоштовно, спільнота, інше.
- 🔍 **Пошук і фільтри** — за словом, містом/індексом, ціною, лише безкоштовні, сортування.
- 🖼️ **Фото** — до 8 на оголошення, стискаються в браузері; у стрічці **не показуються**, тільки на сторінці оголошення (як у вимозі).
- ☎️ **Контакти в один тап** — подзвонити, написати у WhatsApp або Telegram.
- ⭐ **Збережені** та 👤 **Мої оголошення** (з редагуванням і видаленням за токеном).
- 🌙 **Світла / темна тема**, 📦 **офлайн-режим** і встановлення як застосунку (PWA).

## Запуск

Потрібен лише Node.js 18+ (жодних залежностей встановлювати не треба):

```bash
npm start
# або
node server.js
```

Відкрийте <http://localhost:3000>.

Змінні оточення: `PORT` (типово 3000), `HOST` (типово 0.0.0.0).

## Структура

```
server.js              сервер: статика + REST API + JSON-сховище
data/listings.json     демо-дані (seed) — копіюються у listings.runtime.json при першому старті
public/
  index.html           оболонка застосунку
  css/styles.css       стилі (теми, адаптив)
  js/app.js            роутер, тема, події
  js/views.js          екрани (головна, пошук, деталі, форма, збережені, мої)
  js/api.js            клієнт API + localStorage
  js/data.js           категорії, міста, утиліти
  manifest.webmanifest, service-worker.js, icons/   PWA
scripts/gen-icons.mjs  генерація PNG-іконок без залежностей
```

## API (коротко)

| Метод  | Шлях                  | Опис                                  |
|--------|-----------------------|---------------------------------------|
| GET    | `/api/listings`       | список + фільтри (`q,category,city,min,max,free,sort,page`) |
| GET    | `/api/listings/:id`   | одне оголошення (рахує перегляди)     |
| POST   | `/api/listings`       | створити (повертає `editToken`)       |
| PUT    | `/api/listings/:id`   | редагувати (потрібен `editToken`)     |
| DELETE | `/api/listings/:id`   | видалити (потрібен `editToken`)       |

Фото надсилаються як `data:`-URL у полі `images`; сервер зберігає їх у `public/uploads/`.

## Примітки

- Сховище — простий JSON-файл, чудово для прототипу / невеликого спільнотного сайту. Для масштабування варто замінити на БД (Postgres/SQLite).
- Право на редагування підтверджується токеном, який зберігається в `localStorage` пристрою.
